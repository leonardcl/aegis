Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
WTForms==3.1.2
python-dotenv==1.0.1
PyYAML>=6.0
gunicorn==22.0.0
# Optional — only needed for STRIPE_LIVE=1 (reconcile against real Stripe test mode).
stripe>=9.0
