# Aegis CFO — Roadmap & Maturity Plan ("The Ultimate Map")

> **Aegis CFO** — an autonomous AI back-office that monitors spend, runs procurement,
> audits its own books, and *physically cannot exceed the financial mandate you give it*,
> because every dollar it moves passes a default-deny guardrail it can't override.
>
> **Principle:** *constrain actions, free cognition.* A smarter agent inside a sound
> action-boundary is the goal.

This document is the single source of truth for taking Aegis CFO from a strong hackathon
prototype to a **mature, sellable, flawless** product. It is evidence-based: the current-state
scorecard below is the verified output of a multi-agent review (6 reviewers → adversarial
verification → synthesis), cross-checked against the source.

---

## 1. Current state (verified 2026-06-24)

**Verdict: 6.5 / 10 — genuinely impressive engineering, not yet "great."** Strong, stable,
demoable, and far past a mockup — but it drifted off its own plan in the places judges and
buyers look hardest.

| Axis | Score | Note |
|------|-------|------|
| Usefulness | 7 | Real problem, real autonomy story, breadth over-delivered |
| Viability | 6 | Live Nemotron reasoning is a real asset; **Stripe is still mocked** |
| Presentation | 6.5 | Polished shell; chatbot persona leak + missing hero-loop beats |

**What's genuinely strong (protect this spine — do not rewrite):**
- ✅ **Live multi-agent Audit Council** on real Nemotron via Hermes (`:8642`), end-to-end in
  ~38–76s (`engine=hermes`). Catches the seeded rogue $450 charge + $300 mismatch, passes
  compliance, synthesizes savings. The numbers are rule-computed; the LLM only narrates.
- ✅ **Deterministic 7-step audit engine** (gather → reconcile → compliance → period →
  categorize → report → escalate).
- ✅ **Full procurement pipeline** + agent-vs-agent Tier-1 negotiation.
- ✅ **Guardrail philosophy** (cognition free / money gated / self-modification denied),
  articulated more sharply than the plan; live chat correctly refuses a $90k autonomous payment.
- ✅ **64/64 tests pass**, all pages 200, stable.

---

## 2. Definition of done — what "mature, sellable, flawless" means here

A reasonable buyer/judge can run it and find **no broken paths, no false claims, no unsafe defaults**:

1. **Safe** — no debug RCE, no anonymous spend-approval, secrets fail-closed, CSRF-protected,
   inputs never 500 the page.
2. **Accurate / precise** — the guardrail enforces *exactly* what its prompt and docs advertise
   (allowlist + per-txn cap + daily/monthly budget + approval threshold). No claim the code can't back.
3. **Autonomous & agentic** — there is a real "agent finds waste and acts" loop, not just human CRUD.
4. **Consistent** — one demo fiction; every number (seed, dashboard, docs, video) agrees; the
   seeded audit report is generated by the engine so it can't drift.
5. **Honest** — README/docs match reality (Hermes is LIVE), no stale "mocked" framing.
6. **Reversible** — under version control; a dev-mode guardrail bypass that defaults OFF and is loud.
7. **Deployable** — runs under a real WSGI server; reproducible env; clear run/deploy docs.

---

## 3. Phased execution plan

Ordered by ROI and risk. Each phase ends with `pytest` green + a commit. The working spine is
never rewritten — changes are additive/surgical.

### Phase 0 — Safety & Foundation ✅ DONE
- [x] **0.1** Version control: `git init`, `.gitignore` (secrets/venv/db/logs excluded), baseline on `main`, work on `dev-hardening`.
- [x] **0.2** Env-gate the dev server: `run.py` reads `FLASK_DEBUG` (default **OFF**), `HOST`/`PORT` from env. Kills the public-tunnel RCE.
- [x] **0.3** Fail-closed `SECRET_KEY`: random per-process key when unset (no known-constant key).
- [x] **0.4** Guardrail **dev-toggle** `GUARDRAILS_DISABLED` (default OFF, loud `dev_bypass`, startup warning). Gates `evaluate_policy` + `screen_reply`; self-modification BLOCK stays denied.
- [x] **0.5** Optional shared-password Basic-Auth shim (`AEGIS_BASIC_AUTH`, default OFF).

### Phase 1 — Correctness ✅ DONE
- [x] **1.1** Approval idempotency + status guard: `decide_approval` early-returns unless `NEEDS_APPROVAL` (route + service). Stops force-posting a BLOCKED item and double-posting.
- [x] **1.2** Reconciliation-poison fix: approved spend gets a `ch_aegis_` Stripe-style id + a confirmed twin derived from the ledger; approve-then-audit no longer fabricates a discrepancy.
- [x] **1.3** Numeric input hardening: `parse_money()`/`parse_int()` tolerate `$`/commas/`k`/`m`; no 500 on `"$30,000"`.
- [x] **1.4** Chatbot persona-leak fix: hardened `SYSTEM_PROMPT` + canned identity reply + `scrub_identity` (vendors preserved).
- [x] **1.5** Chatbot latency UX: shorter `HERMES_CHAT_TIMEOUT` + client AbortController + send-disable.
- [x] **1.6** CSRF protection: same-origin Origin/Referer guard on state-changing routes (`/hermes` exempt); per-form tokens deferred to 5.x.

### Phase 2 — Guardrail completeness ✅ DONE
- [x] **2.1** Default-deny **payee allowlist** (env-configurable): unlisted payee → NEEDS_APPROVAL (human vets a first-time payee; the agent never auto-pays a stranger). Blocklist → BLOCK.
- [x] **2.2** **Per-transaction cap** (BLOCK) distinct from the approval threshold.
- [x] **2.3** Ledger-backed **daily / monthly budget** accumulator inside `evaluate_policy` (UTC). Also fixed a real `date.today()`-vs-`utcnow()` day-drift bug.
- [x] **2.4** Boundary tests: allow/needs-approval/block, allowlist, per-txn cap, daily+monthly budget, negative.

### Phase 3 — Autonomous hero loop + demo coherence `[the autonomy story]`
- [ ] **3.1** "**Run daily review**" endpoint + service: ingest a mock usage feed → flag a zero-usage subscription → propose `cancel_subscription` → guardrail ALLOW → ledger entry with `$X/mo saved`.
- [ ] **3.2** Seed demo artifacts: a small ALLOW beat under the cap, an allowlist-BLOCK beat, a green auto-approved ledger row on the dashboard.
- [ ] **3.3** Data consistency: pick ONE fiction; rebuild the seeded `AuditReport` by calling `full_audit()` so it can't drift from the live engine.

### Phase 4 — Presentation & packaging `[sellable surface]`
- [ ] **4.1** README rewrite: Hermes/Nemotron is **LIVE**; remove stale "mocked" framing; quick-start + architecture.
- [ ] **4.2** Dashboard 3-beat polish so the autonomy → save → block story lands on one screen.
- [ ] **4.3** `DEMO_SCRIPT.md` — the 2-minute shot list mapped to real clicks.

### Phase 5 — Maturity / future work `[ready to sell beyond the hackathon]`
- [ ] **5.1** Real **Stripe test-mode** read-only fetch behind `STRIPE_LIVE=false` (map `Charge.list`/`PaymentIntent.list` to the `get_charges` shape). *Sponsor tie-in; do not chase money-moving/webhooks under deadline.*
- [ ] **5.2** Real **NemoClaw-style policy file**: out-of-process `agent-cfo.policy.yaml` the gate reads, so "the agent can't edit its own policy" is concretely true and screenshot-able.
- [ ] **5.3** **Deployment**: `gunicorn -w 1` (respects Hermes serialization), `Dockerfile`, env-driven config, healthcheck.
- [ ] **5.4** **AuthN/Z & multi-tenant**: real login, per-org budgets/policies, role-gated approvals.
- [ ] **5.5** **Observability**: structured logs, request ids, an audit-of-the-audit trail, metrics.
- [ ] **5.6** **CI**: GitHub Actions running `pytest` on push; coverage gate.
- [ ] **5.7** **Event-driven trigger**: Stripe webhooks / scheduled monthly audit (the locked "on-demand + scheduled" decision).

---

## 4. Risk register (carried from the review)
- **Don't break the working demo** — the spine is stable and on-thesis; keep changes surgical. If time is tight, ship Phases 0–4 and stop; Phase 5 is additive.
- **Model latency on demo day** — pre-run the council before filming; lower chat timeout; disable chat send during a council job.
- **Plan/build drift** — one fiction, consistent numbers, engine-generated seed report.
- **Untested write paths** — add boundary tests alongside the Phase 1–2 fixes.

---

*Maintained alongside the code. Checkboxes are updated as phases land; each phase is a commit on
`dev-hardening`. Generated and executed with multi-agent review + Claude Code.*
